/*
#include <stdio.h>
#include <stdlib.h>

int sumRange(int start, int end) {
    int i;
    int sum = 0;
    for (i = 0; i < end+1; i++){
        sum += i;
    }
    return sum;
}
//int main(int argc, char** argv){
//    if (argc != 3){
//        fprintf(stderr, "usage: %s\n start end\n", argv[0]);
//        return 1;
//    }
    //printf("3");

int main(){
//    char a = 'A';
//    char b = a + 1;
//    char c = 67;
//    char little_g = 'G' - 'A' + 'a';
//
//    printf("'%c' = %d, '%c' = %d, '%c' = %d, '%c' = %d", a, (int)a, b, (int)b, c, (int)c, little_g, (int)little_g);
//    printf("\n");
 
 
 
//    char* test = "This is a test.";
//    printf("\"%s\" lives at address %d and the first character is %c.\n", test, &test, *test);
//    printf("\"%s\" lives at address %d and the first character is %c.\n", test, test, *test);
 
 
 

    int n = 37;
    printf("n lives at address %d and it contains the value %d.\n", &n, n);
    char* format1 = "n lives at address %d and it contains the value %d.\n";
    printf(format1, &n, n);
    
    printf("\t%d %d %d %d %d %d\n", -3/3, -2/3, (-2)/3, -4/3, (-4)/3, 2/3);

 return 0;
}
 */

